import styled from "styled-components";

/** Contenedor flex reutilizable (display flex, justify/align center, gap 5px) */
export const ContenedorFlex = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 5px;
`;
